heatmap_mapper_node:
  ros__parameters:
    # Output Configuration
    output_dir: "logs"
    output_file: "fused_heatmap.csv"
    
    # Data Filtering
    target_ssid: ""  # Empty string = log all networks. Set to specific SSID to filter
    min_rssi: -90    # Minimum RSSI threshold (dBm)

