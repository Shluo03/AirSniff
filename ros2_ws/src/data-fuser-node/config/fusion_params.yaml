heatmap_mapper_node:
  ros__parameters:
    # Output Configuration
    output_dir: "logs"
    output_file: "fused_heatmap.csv"
    
    # Data Filtering
    target_ssid: "FAF-HQ-GUEST"  # Target SSID to track. Leave empty "" for all networks
    min_rssi: -90                 # Minimum RSSI threshold (dBm)

