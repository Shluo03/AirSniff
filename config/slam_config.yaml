# File: config/slam_config.yaml
# Placeholder configuration for pycuVSLAM and camera parameters

camera:
  # TODO: Fill with actual camera intrinsics/extrinsics
  model: pinhole
  width: 1280
  height: 720
  fx: 600.0   # TODO
  fy: 600.0   # TODO
  cx: 640.0   # TODO
  cy: 360.0   # TODO
  distortion_model: radtan  # TODO
  distortion_coeffs: [0.0, 0.0, 0.0, 0.0]  # TODO
  frame_id: camera_link
  # For stereo: set use_stereo true and add right camera intrinsics and baseline

pycuVSLAM:
  # TODO: Fill with actual cuVSLAM parameters
  use_stereo: false
  enable_loop_closure: true
  enable_relocalization: true
  max_features: 2000
  # Add more cuVSLAM-specific parameters as needed

