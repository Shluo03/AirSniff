# Minimal Python deps for development (ROS 2 often provides these via apt)
# NOTE: For ROS 2 environments, prefer installing via apt/rosdep.

rclpy
numpy
PyYAML
pymavlink

# TODO: image handling; install per-platform if needed
# opencv-python

# TODO: ROS 2 launch packages (usually from ROS distro, not pip)
# launch
# launch_ros

# NOTE: pycuVSLAM is installed separately following NVIDIA instructions.

