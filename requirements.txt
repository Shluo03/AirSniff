# Minimal Python deps for development (ROS 2 often provides these via apt)
# NOTE: For ROS 2 environments, prefer installing via apt/rosdep.

rclpy
numpy
PyYAML

# Image handling
opencv-python  # required for video recorder and offline recon

# Depth Anything V3 pipeline
# IMPORTANT: On Jetson, install torch/torchvision matching your JetPack via NVIDIA wheels.
# torch
# torchvision
depth-anything-3

# TODO: ROS 2 launch packages (usually from ROS distro, not pip)
# launch
# launch_ros

# Offline reconstruction (install separately; heavy deps)
# torch
# torchvision
# open3d
# colmap  # installed via system package manager; not pip
