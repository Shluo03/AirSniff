# File: scripts/README.md

Helper scripts (TODO)
- Add setup helpers, e.g., environment sourcing, camera bring-up, or data collection scripts.
- Example ideas:
  - setup_udev_rules.sh (for camera access)
  - run_local_launch.sh (wrapper to ros2 launch)
  - export_camera_intrinsics.py (dump camera YAML)

